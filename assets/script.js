const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelector('#year').textContent=new Date().getFullYear();document.querySelector('#volunteer-form')?.addEventListener('submit',event=>{event.preventDefault();const form=new FormData(event.currentTarget);const note=document.querySelector('#form-note');const subject=encodeURIComponent('AWILO campaign — volunteer interest');const body=encodeURIComponent(`Name: ${form.get('name')}\nEmail: ${form.get('email')}\nInterest: ${form.get('interest')}\n\nPlease replace the campaign email placeholder before publishing.`);note.textContent='The official campaign email is not yet confirmed. Your details have not been stored.';window.location.href=`mailto:?subject=${subject}&body=${body}`});
