(()=>{'use strict';const header=document.querySelector('header,.page-header');const progress=document.createElement('div');progress.className='scroll-progress';document.body.prepend(progress);const top=document.createElement('button');top.className='back-top';top.type='button';top.setAttribute('aria-label','Back to top');top.textContent='↑';document.body.append(top);top.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));const update=()=>{const y=scrollY;header?.classList.toggle('header-scrolled',y>18);top.classList.toggle('visible',y>500);const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?Math.min(100,y/max*100):0)+'%'};addEventListener('scroll',update,{passive:true});update()})();
